# Dependencies (installed in the image)
node_modules/

# Local / generated
logs/
dist/
build/
coverage/
*.tsbuildinfo

# Git and editor
.git/
.gitignore
.vscode/
.idea/

# Secrets and local env (set via Render env / secret files)
.env
.env.*
!.env.example

# Do not bake local SQLite state; schema.sql is still copied (not matched here).
data/*.db

# Local-only artifacts from repo
optimization-results/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Docs and noise (optional shrink)
*.md
!README.md

# Debug / scratch
debug-bitunix.ts
out.txt
tsc.log
